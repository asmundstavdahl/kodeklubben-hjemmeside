{% if sponsors|length > 0 %}
    <div class="box box-info sponsorer">
        <div class="box-header with-border">
            <h3 class="box-title box-title-lg">Kodeklubben Trondheim sponses av</h3>
        </div>
        <div class="box-body">
            {% for sponsor in sponsors %}
                <div class="col-md-2 col-sm-3 hero-feature">
                    <div class="box">
                        <div class="sponsor">
                            <div class="img-container">
                                {% include 'image/editable_image_template.html.twig' with {'image': get_sponsor_image(sponsor.id)} %}
                            </div>
                            <div class="clearfix">
                                <a href='{{ sponsor.url | escape('html_attr') }}'>
                                    <h3>{{ sponsor.name }}</h3>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endif %}
